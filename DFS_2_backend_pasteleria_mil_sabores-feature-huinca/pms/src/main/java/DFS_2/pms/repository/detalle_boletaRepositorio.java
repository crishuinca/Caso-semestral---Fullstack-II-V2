package DFS_2.pms.repository;

import DFS_2.pms.entity.detalle_boleta;
import org.springframework.data.jpa.repository.JpaRepository;

public interface detalle_boletaRepositorio extends JpaRepository<detalle_boleta, Integer>{
    
}
